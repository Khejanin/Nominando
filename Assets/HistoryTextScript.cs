using TMPro;
using UnityEngine;

public class HistoryTextScript : MonoBehaviour
{
    private TextMeshProUGUI textMesh;
    
    // Start is called before the first frame update
    void Start()
    {
        textMesh = GetComponent<TextMeshProUGUI>();
    }
    
}
